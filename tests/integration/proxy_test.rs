/// End-to-end proxy integration tests with a mock upstream HTTP server.
///
/// Tests verify that:
/// - The upstream receives tokenized (sanitized) content.
/// - The caller receives detokenized (original) content in the response.
// TODO(next-session): Implement after server.rs, proxy.rs, and vault are complete.
